# Nebula 功能对照

本项目以 `Kuddev/nebula` v1.3.0（main 提交 `1960e4ae`，2026-08-24 发布）作为行为对照，覆盖 v1.2.0（提交 `74f19d8`，2026-08-21）与 v1.3.0 的全部功能；2026-08-25 复查上游发布与 main 分支，v1.3.1 已发布（头号新增：应用内验证更新）。Nebula 使用 GPL-3.0；本项目只分析公开功能和交互，并把其 GPUI 界面当作"设计图纸"做数值对标（间距、圆角、阴影、动效时长、配色 token），全部在 React/CSS 中独立实现，不复制其 Rust 源码、图片或其他 GPL 资产。

## 当前覆盖

| 能力 | 状态 | 本项目实现 |
| --- | --- | --- |
| 常驻终端与标签恢复 | 已实现 | Electron 主进程管理 ConPTY，隐藏窗口不断开，保存并恢复标签元数据与终端回放 |
| 标签、分屏与拖拽停靠 | 已实现 | 标签排序、中键关闭、四分屏、窗格缓存、拖拽停靠 |
| 水平标题栏标签页（v1.2） | 已实现 | 顶部模式支持选择、拖拽排序、停靠、分屏与水平自动滚动；设置入口收进三点菜单；首个标签与终端/powerline 左缘对齐；侧边栏模式不变 |
| 分页标题栏标签与 pane 标题栏（v1.3） | 已实现 | 顶部标签溢出后显示前后翻页控件（pageTopTabs）；分屏 pane 有可拖动标题栏，可拖回标签栏还原为标签 |
| 按标签广播输入（v1.3） | 已实现 | 焦点 pane 的字节与控制键转发到同标签全部 pane；pane 数大于 1 时显示广播开关 |
| 新建标签位置 | 已实现 | 设置项可把新标签放在当前标签之后或追加到末尾，随设置持久化 |
| 标签栏位置 | 已实现 | 与 Nebula `TabsPositionName` 一致，仅 顶部 / 侧边 两种互斥布局（默认侧边），切换即时生效；避免顶部与侧栏两个三点菜单同时出现 |
| 标签页颜色标记 | 已实现 | 右键标签选 7 色标记或清除，localStorage 持久化，顶栏与侧栏均显示色条 |
| 标签页重命名与导出 | 已实现 | 右键标签重命名；导出当前标签会话为文本文件（保存对话框，缓冲区内容写入 UTF-8） |
| 退出码与失败状态 | 已实现 | 主进程捕获真实退出码随 exit 事件下发；终端内显示 `process exited (code N)`，非零退出时标签出现失败图标与退出码文案，托盘菜单显示"失败" |
| AI 回合四状态识别 | 已实现 | 侧栏/顶栏标签显示 AI CLI 运行中、已完成、等待输入、失败四种状态（转圈/圆点/三角/红叉），空闲无圆点 |
| AI 回合状态看门狗（v1.3，#42/#54） | 已实现 | 3 秒周期进程树探测，Codex/Claude 回到空闲提示符后推翻过期 running 状态；解决"命令结束仍显示运行中"与"命令执行莫名中断" |
| AI 完成通知 90 秒关闭（v1.3） | 已实现 | 通知节流并跟踪，90 秒后自动关闭，避免无限堆积 |
| AI 会话跨重启恢复与分叉 | 已实现 | 恢复纯 Shell 标签时按快照注入 `codex resume <id>` / `claude --resume <id>`；右键运行中的 AI 标签可"继续上次 AI 会话"或"分叉 AI 会话"，设置页可关闭自动接续 |
| Shell 与 CLI 启动 | 已实现 | PowerShell、CMD、Git Bash、WSL、SSH；Codex、Claude Code、Gemini、OpenCode、DeepSeek Harness 和任意自定义命令 |
| AI CLI 活动同步 | 已实现 | Codex `config.toml/notify`、Claude hooks、链式配置保护、自动修复和点击聚焦 |
| Shell 分类注入（v1.2） | 已实现 | PowerShell、cmd、Nushell 与 fallback 的分类注入与检测（electron-shells 真实探测 Nushell）；已移除注入的 `fmt` 动作 |
| Shell 配置文件入口（#31） | 已实现 | 设置页"默认 Shell"行提供"打开配置文件"按钮，覆盖 PowerShell/pwsh/Nushell/bash/zsh（shell:profile-path / shell:open-profile） |
| WSL 来宾默认 shell（v1.2，#44） | 已实现 | `wsl --distribution <distro>` 启动，不覆盖来宾账号默认 shell；WSL 发行版非阻塞探测 |
| WSL 文件树刷新（v1.2） | 已实现 | 多个 WSL 根目录合并为一次来宾 `find`，冷启动友好超时与事件驱动刷新；文件面板跟随本地/WSL/SSH pane |
| WSL 快捷入口（#12） | 已实现 | 空工作台“WSL 发行版”入口列出已注册发行版，一键以 `\\wsl.localhost\<发行版>` 为工作目录建立项目会话；常用目录面板提供 WSL 根目录快速入口，目录选择器保留原生系统对话框 |
| 补全路由（v1.2） | 已实现 | 本地、WSL 与 SSH pane 各自查询自己的文件系统（getCompletions 按会话 cwd/sshProfile 路由） |
| 终端日常交互 | 已实现 | 选中复制、右键复制/粘贴、搜索、缩放、链接、行内图片、本地文件安全拖入 |
| 终端链接与预览 | 已实现 | 终端内文件路径与 URL 由 xterm link provider 标出下划线，Ctrl+悬停显示文件/目录预览卡片，Ctrl+点击在系统资源管理器中打开路径或调起浏览器打开 URL |
| 剪贴板截图粘贴 | 已实现 | 剪贴板无文本但有图片时写入临时 PNG，并粘贴带引号的本地路径到终端；SSH 会话通过 SFTP 上传到远端 /tmp 后粘贴远端路径 |
| 文本粘贴修复（#65） | 已实现 | 新增主进程 `clipboard:read-text` IPC，渲染进程 `navigator.clipboard` 权限失败时兜底；SSH/普通终端均可粘贴文本与密码 |
| IME 输入法（v1.2，#43） | 已实现 | 普通字母与空格不绕过 Windows text-input 路径，composition 交给 xterm 处理，搜狗等输入法可正常确认组合文本 |
| Ctrl+C 复制语义（v1.2，#48） | 已实现 | 有选区且启用复制时 Ctrl+C 才复制；无选区时 Ctrl+C 直接透传给终端，不吞键、不重复复制旧选区 |
| 弹窗模式历史方向键（v1.3，#56） | 已实现 | popup 补全模式无候选时 Up/Down 回退为终端历史循环，可一直按上方向键切换历史记录 |
| PTY 读取唤醒重臂（v1.2） | 已实现 | PTY 数据事件持续 drain，pending 数据会重新注册读取唤醒，输出不卡半帧 |
| 终端铃声模式 | 已实现 | 关 / 仅闪烁 / 仅声音 / 闪烁 + 声音 四档，BEL 节流 1.5 秒 |
| 命令补全 | 已实现 | 内联建议与弹窗候选两种样式；补全对 WSL 路径使用超时保护的只读目录扫描，避免冷启动挂起 |
| 语义化补全弹窗（v1.3） | 已实现 | 历史/命令/目录/文件各有独立来源标签、图标与颜色；光标靠近底部时浮层向上翻转；绘制、点击、悬停与滚动共用同一几何 |
| 弹窗补全列表几何 | 已实现 | 对齐 Nebula 旧壳 `draw_completion_popup`：8px 圆角面板 + 4px 内边距 + 菜单级阴影；候选行 `图标+命令+来源标签` 布局，选中行强调色水洗底 + 墨色粗体 + 6px 圆角 |
| 命令面板 | 已实现 | 分组搜索、最近使用、终端切换、分屏、工作区、SSH 与设置动作 |
| 命令面板几何 | 已实现 | 对齐 Nebula `render_command_palette`：580×520 面板、10px 圆角、8px 内边距、距顶 76px；分组头 26px、行高 36px、行圆角 8px、图标槽 22px、选中行强调色水洗底 |
| 文件与开发工具 | 已实现 | Files、Git、Markdown/GFM/KaTeX、JSON/文本预览；文档内本地图片经 IPC 转 data URI 展示，远程图片直连加载，图片加载失败显示占位 |
| 独立图片标签查看器 | 已实现 | 图片单独开文档标签查看（readDocumentImage IPC），Electron 集成测试覆盖 |
| 终端内分屏文件视图（#53） | 已实现 | 非图片文档在活动终端右侧分屏打开（document pane），可关闭/拖拽调整；图片仍单独开标签；文档 pane 不写入布局快照 |
| 终端内 TeX 公式渲染（#55） | 已实现 | 识别 $...$ / $$...$$ / \\(...\\) / \\[...\\] 公式（含跨软换行与 Agent TUI 硬换行），保守恢复被剥定界符的 (...)/[...] 公式，行断折叠与 \frac13 归一化后用 KaTeX 渲染；暗色推理行保持原文；覆盖层不修改 buffer，选区/复制仍是原始 TeX；设置页可开关（terminal-math 单元测试覆盖） |
| 文本查看器行号 | 已实现 | 非 Markdown 文件按行渲染并显示行号，长行保留横向滚动 |
| SVN 仓库状态与操作工作流（v1.2） | 已实现 | VCS 面板 SVN 状态解析、作用域过滤与操作翻译（vcs 单元测试 + workflow 集成覆盖） |
| Git 仓库兼容性 | 已实现 | git 命令加 `-c safe.directory=*`，修复 WSL 或其他用户所有仓库显示空白 Git 面板的问题 |
| 目录树/GIT 左键（#62） | 已实现 | 目录行整行可点击，左键单击任意空白处即进入目录；按钮区排除，避免与展开/菜单冲突 |
| 分栏线命中区（#61） | 已实现 | pane 分栏线伪元素 `inset:-3px` 扩大触发区域，拖拽位置与显示一致 |
| SSH 与 SFTP | 已实现 | 系统 OpenSSH、ssh config 导入、连接测试、交互终端和基础 SFTP 操作；活动 SSH pane 时文件面板自动切换为 SFTP |
| SSH 失败重试（v1.2） | 已实现 | 仅失败后在"关闭"左侧显示"重试"，原位替换失败的 pane；pane 工具栏与侧栏标签一致 |
| SSH 旧格式探测（v1.2） | 已实现 | `user@host:port` 仅在 `ssh -G` 探测时规范化为 `ssh://user@host:port`，避免 Windows 探测报 11001（electron-ssh-format 测试） |
| Runtime 控制与编排（v1.2） | 已实现 | 主进程 Runtime 控制：命名 agent、进程/控制键命令、真实退出码与 pane 生命周期事件；随包附带 runtime schema（docs/runtime-api-v1.schema.json）、API 文档与 `nebula-runtime` Skill |
| 事务式托管 agent worktree（v1.2） | 已实现 | agent fork 保留稳定分支/路径身份，prepare/commit/rollback 事务式创建，dirty-source 拒绝提交（git-worktree + electron-agent-fork 测试） |
| 更新检查（#47） | 已实现 | 设置页"关于"区提供检查更新按钮（GitHub releases API、8s 超时），显示最新版本、安装包清单与发布说明声明的 SHA-256 |
| 应用内验证更新（v1.3.1） | 已实现 | 应用内后台下载官方 Windows 安装包（优先 Setup→Portable→任意 exe），流式上报进度条；校验大小、PE 头（MZ+PE）与 SHA-256（发布体或 .sha256 资产），校验失败自动删除并报错；通过后才显示"确认安装"，且只允许启动 userData/updates 目录内的 exe；update-manager 单元测试覆盖 |
| 更新通知常驻（v1.3.1） | 已实现 | 检查更新后，右下角常驻提醒持续显示直至用户处理：下载/校验中显示进度条，完成/失败原位替换原提醒；提供忽略/重试/确认安装操作；设置页"关于"区与全局 toast 消费同一份更新状态（状态提升至 App，SettingsPanel 只读）；与完成/错误通知共存时自动上移避让，不重叠遮挡；workflow 集成测试覆盖完整流转 |
| 外观与交互设置 | 已实现 | 七套主题、背景图片、透明度、模糊、光标、可配置字体族（含中文回退链）、面板宽度、补全、Powerline、三档界面密度 |
| 七主题色板数值 | 已实现 | 七主题 shell/term/panel/pill/ink/accent/语义色与 Nebula `display/ui/theme.rs` 的 palette+skin 数值一一对应（独立实现）；浅色主题 ANSI-16 用 GitHub Primer Light 色表，窗口标题栏覆盖层同步 shell 色 |
| 控件几何与动效 | 已实现 | 开关 48×26 / 旋钮 20 / 位移 24（400ms 液态 + 250ms 拉伸 + 300ms 变色）、菜单与补全浮层圆角 8、浮层阴影 4/12、侧栏标签行 34/42 |
| 侧栏与抽屉几何 | 已实现 | 左侧栏默认 230px、范围 170–420px、拖至 120px 以下折叠；右侧抽屉默认 300px、范围 220–560px、最大占窗口 42%、拖至 150px 以下关闭；对应 Nebula `display/mod.rs` 与 `side_panel.rs` 的 SIDEBAR/PANEL/DRAWER 常量 |
| 设置页两栏布局对标 | 已实现 | 左侧 196px 分区导航（顶部 72px 品牌区、导航项 32px、圆角 8、图标+文字、active 为强调色水洗底），右侧固定 72px 分区标题头（含还原/关闭按钮），正文单独滚动；分组标题 26px + 16px 间距，组间 32px 居中 1px 分隔线；设置行高 44px（紧凑 38px）、左右 16px 行内缩进，全部对齐 Nebula `settings_pane.rs` 的 SETTINGS_NAV_WIDTH/HEADER/GROUP_GAP/ROW_HEIGHT |
| 键位页 | 已实现 | 可录制自定义键位（命令面板、新终端、分屏、全局快速终端、打开设置），支持搜索与冲突检测，一键恢复默认 |
| 非默认值标记（v1.3） | 已实现 | 设置页逐行显示"已修改"圆点（data-key 标识），与默认值比对，改回默认即消失；开关与选择/输入行同步标记，设置页顶部外观分区另有主题标记 |
| 字体文件导入 / 私有字体安装 | 已实现 | 设置页可导入 .ttf/.otf/.ttc/.otc（≤64 MB），按内容 SHA-256 去重存入 userData/fonts；直接解析 sfnt name 表取族名（优先 nameID 16/1，支持 TTC/OTC），经 font:// 协议注册 @font-face，终端与字体选择器立即可用；选择器列表带「导入」标记、默认等宽视图可见，导入列表可逐项删除（font-import 单元测试覆盖） |
| 所见即所得字体选择器 | 已实现 | 首次展开时通过真实 Electron IPC 异步枚举系统字体并缓存；220px 触发条、400px 浮层、最大 320px 列表与 36px 候选行对齐 Nebula 常量，每个候选名用自身字体渲染，支持搜索、仅等宽/显示全部筛选；选择只替换主字体并保留原有逗号 fallback 链 |
| 三点菜单设置入口 | 已实现 | 顶部标签栏 `+` 右侧紧跟竖三点，菜单仅"选择终端 / 设置"两项，180px 浮层锚定在按钮下方；侧栏标题右侧为 20×20 的 `+` 与竖三点；设置页以单例标签打开 |
| 自定义颜色取色盘 | 已实现 | 背景色与界面强调色各配一套 HSV 连续取色器（饱和度/明度取色面 + 色相条），终端背景与强调色实时预览 |
| 任务栏与 Alt+Tab 图标（v1.3，#51） | 已实现 | `app.setAppUserModelId` + BrowserWindow 图标 + 打包 win.icon（build/icon.ico，脚本生成几何图形图标），任务栏悬停预览与 Alt+Tab 不再显示默认占位图标 |
| 托盘 agent 提醒 | 已实现 | 后台 AI CLI 需要处理时托盘切换为琥珀色提醒图标，点击托盘聚焦对应终端；图标为程序生成的几何图形 |
| 托盘 agent 菜单 | 已实现 | 托盘菜单实时列出所有终端（等待输入/运行中/已退出/空闲），点击直达对应窗格 |
| 中英文界面 | 已实现 | 跟随系统、简体中文、English；产品术语按使用场景编写，非机械直译 |
| 加密工作区导出 / 恢复（v1.3） | 已实现 | NEBUBAK1 格式：口令（至少 8 位）+ 随机盐 + AES-256-GCM（AAD=magic），Argon2id 参数用 scrypt(N=2^14,r=8,p=1) 近似；分类白名单（外观/CLI 配置/SSH 脱敏/会话/目录历史/命令历史/字体），导出前剥离代理凭据与 SSH 私钥；恢复先全量解密校验（magic、版本、分类、路径防穿越、内容合规）再逐文件原子写；恢复前预览（列出将覆盖/新建的文件与大小），覆盖前先把原文件挪到 .codex-cli-ui-backup-<时间戳> 安全副本目录，中途失败自动回滚并清空安全目录，成功后保留安全副本并热重载设置与主机列表；设置页「备份」分区（恢复预览 + 确认恢复）+ 中英文案 |
| SSH 高级能力（代理 / 跳板 / 保活） | 已实现 | 主机编辑器提供高级设置区：ProxyJump 跳板机（单跳，-J）、ProxyCommand 代理命令（与跳板互斥、跳板优先）、ServerAlive 保活（间隔与最大失败次数，0=关闭、留空沿用 ssh_config）、PreferredAuthentications 认证偏好；ssh 会话、SFTP 批处理与连接测试统一使用同一套传输参数；ssh-utils 单测覆盖参数组装与非法输入 |
| 内置制表符字形 | 已实现 | 边框/方块/象限字符按几何路径绘制（BoxGlyphOverlay，对标 Nebula builtin_font/glyphs.rs）：单线/粗线/双线/虚线/弧角/对角/方块元素/象限块/阴影，线宽 = max(1, round(格宽/8))，粗线 ×2；覆盖层随终端写入/滚动/缩放重绘，颜色取自 cell 前景色（RGB / 256 色板 / 主题前景），dim 半透明、反相格跳过、devicePixelRatio 适配；设置页「终端」分区可开关 |
| 拖拽选择自动滚动 | 已实现 | 按住左键拖动选择并把指针拖出终端网格上/下边缘时，视口按 15ms 节奏持续滚动：边缘 1 行、每 20px +1 行、上限 16 行/拍；鼠标按下即武装计时器，快速甩出边缘也能触发；松开（含在标签条/窗口外）或窗口失焦立即停止，不残留粘滞选择（对标 Nebula 1.3.0） |
| 统一确认弹窗（v1.3.1，#64） | 已实现 | 工作区危险运行、SFTP 删除、更新安装与 SSH 主机编辑器全部走同一 ConfirmDialog：Esc、遮罩空白点击、取消按钮三条路径行为一致地取消；确认按钮独立执行，危险操作带图标与强调色确认；默认焦点落在取消按钮防止误触发；SSH 编辑器同样支持 Esc 关闭（workflow 集成测试覆盖四种取消/确认路径） |

## 部分覆盖

| 能力 | 当前限制 |
| --- | --- |
| 崩溃恢复 | 有启动跟踪、恢复隔离和标签快照；隔离快照支持一键恢复（按原 ID/cwd 重建终端并复位失败计数）且只保留最近 3 份；设置页“关于”已加入诊断区（运行时长、活动终端数、连续异常启动次数、隔离快照状态、启动日志目录）与输入延迟探针（向 PTY 写入唯一标记并测回显，测量输入到回显的真实往返延迟） |
| 背景材质透明度（v1.3，#60） | Electron 用 `setBackgroundMaterial("mica")` + 背景图面板蒙层透明度（35–100%）对应；终端正文始终纯色，规避 Nebula 正文透明度导致的可读性问题 |
| 等宽字体渲染（#58） | 支持逗号分隔字体 fallback 链与所见即所得字体选择器；渲染走 xterm canvas，等宽字体故障未在本项目复现 |
| GPU 界面壳 | 当前 Electron/Chromium 默认使用 GPU 合成且只有一套 Nebula 风格界面；没有改用 Rust + GPUI，也没有保留 `--legacy-shell` 双壳。对本项目规模，迁移会重写 xterm.js、node-pty、窗口/托盘/剪贴板与自动化测试，现阶段收益不足 |

## 暂缓项目

- DeepSeek Harness 的完整插件管理和协议仍在变化的高级设置。

## Nebula issues 处理状态

上游 open issues（2026-08-25 快照）逐一核对：

| Issue | 标题 | 本项目状态 |
| --- | --- | --- |
| #66 | SSH Browser（sftp 文件浏览） | 已覆盖：活动 SSH pane 时文件面板自动切换为 SFTP 抽屉（浏览/上传/下载/删除），并与 SSH 会话关联 |
| #65 | SSH 不能粘贴，密码也不能粘贴 | 已实现：剪贴板 IPC 修复 + 截图兜底，普通/SSH 终端均可粘贴 |
| #64 | 通用弹窗提示组件逻辑缺陷 / 确认逻辑缺失 | 已实现：统一 ConfirmDialog（Esc / 遮罩点击 / 取消按钮一致取消，确认独立执行），危险运行、SFTP 删除、更新安装与 SSH 编辑器全部接入 |
| #63 | 拖拽窗口后终端不自适应留空白 | Electron 窗口 resize 触发 xterm fit 自适应，未复现该问题 |
| #62 | 目录树和 GIT 状态鼠标左键单击无效 | 已实现：目录行整行左键可点击 |
| #61 | 拖拽分栏线的触发位置与显示不符 | 已实现：命中区向外扩 3px |
| #60 | 终端正文不透明度调整存在 bug | 审计结论：本项目正文保持纯色，透明度只作用于面板蒙层，规避上游可读性问题 |
| #58 | 等宽字体渲染故障 | 本项目未复现；fallback 链 + 所见即所得选择器已就绪 |
| #57 | sliever 主题下 codex 几乎不可用 | 主题色板数值已对标；需要真实 sliever 环境进一步验证 |
| #56 | 弹窗模式无法用上方向键切换历史记录 | 已实现：popup 无候选时 Up/Down 回退历史循环 |
| #55 | 渲染 WSL agent 的 TeX 公式 | 已实现：终端内 TeX 检测 + KaTeX 覆盖层渲染，跨行公式、裸括号恢复与归一化均有单测 |
| #54 | 命令执行莫名中断 | 已实现：AI 状态看门狗 + PTY 读取唤醒重臂 |
| #53 | 打开的文件在终端分屏里显示 | 已实现：非图片文档在终端 pane 内分屏打开，图片单独开标签 |
| #50 | SSH 连接闪退并导致连不上 | 已提供失败重试与原位替换；真实 SSH 环境下的闪退需进一步验证 |
| #47 | 自动更新或 scoop/winget 更新 | 已实现：应用内下载官方安装包、大小/PE 头/SHA-256 校验、确认后启动安装；scoop/winget 分发已接入：packaging/ 提供 winget 清单与 scoop 清单模板，应用启动时探测 winget/scoop 安装来源（10 分钟缓存），检测到后更新提醒与设置页给出"通过 winget/scoop 升级"入口，在独立控制台启动升级命令，未检测到时回退应用内下载（package-managers 单元测试 + workflow/electron 集成覆盖） |
| #45 | SSH TUI/断连问题 | 已提供失败重试与原位替换；依赖真实 SSH 环境的 TUI 行为需进一步验证 |
| #42 | 检测命令是否结束需要特判 | 已实现：进程树看门狗 3s 探测 |
| #40 | 换行错误 | 未复现；如遇具体场景请提供复现步骤 |
| #31 | 增加 shell profile 按钮 | 已实现：设置页打开配置文件入口 |
| #21 | 启动 PowerShell 7 会卡很久 | 已缓解：WSL/Shell 探测非阻塞并行加载，窗口先行显示；真实慢环境下的启动链路可再优化 |
| #12 | 打开文件夹没有 WSL 路径 | 已实现：空工作台 WSL 发行版快捷入口、常用目录 WSL 根目录、文件面板与补全跟随 WSL 路径 |